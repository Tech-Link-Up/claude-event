# macOS
.DS_Store

# Claude Code local (machine-specific) settings
.claude/settings.local.json

# Safety net for any app folder created anywhere in this repo:
# never commit dependencies, build output, or env files
node_modules/
.next/
.env*
!.env.example
